# Agent instructions

Before editing this portfolio, read every Markdown file in `.plan/`, beginning with
`.plan/HANDOFF.md`. Keep project facts, design decisions, open questions, and continuation notes in
`.plan/` so another session can resume without hidden context.

The site is forward-only and dependency-free unless the user explicitly changes that decision.
Preserve the bilingual language entrance, interactive system constellation, accessibility,
responsive behavior, and reduced-motion support.

